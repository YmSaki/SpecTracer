{
 "run_id": "wf_e9839da5-30f",
 "method": "cluster-stratified adversarial re-verification: 1 representative per cluster + all 9 singletons; opus/effort-high; isolated temp-project repro; repo read-only; contamination check mandatory",
 "sampled": 19,
 "returned": 19,
 "upheld": [
  "VO-ADAPTER-05",
  "VO-AGG-03",
  "VO-APPROVE-07",
  "VO-EXEC-06",
  "VO-EXEC-07",
  "VO-EXEC-09",
  "VO-EXIST-05",
  "VO-EXIST-08",
  "VO-EXIST-09",
  "VO-INTAKE-04",
  "VO-INTAKE-08",
  "VO-PLAN-02",
  "VO-PLAN-04",
  "VO-REPORT-05",
  "VO-SEMAUDIT-02",
  "VO-SEMAUDIT-05",
  "VO-SEMAUDIT-07",
  "VO-STATAUDIT-04",
  "VO-STRUCTOP-09"
 ],
 "overturned": [],
 "tainted": [],
 "unclear": [],
 "attribution_fails": [
  "VO-EXIST-09"
 ],
 "details": [
  {
   "vo_id": "VO-REPORT-05",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachability: OutputFormat::Text is the clap default (lib.rs:41), emit_text is the sole text sink (lib.rs:3854), print_text_tree_node has exactly one call site plus its own recursion — no second renderer. (b) No prior gate: bare `vtest init` + `verify` reaches the tree branch (L3884) and prints; exit 1 is the verdict, not a rejection. (c) Spec: 別紙A L232-234 states verbatim the ancestor-continuation / middle `├─ ` / last `└─ ` rule, applies it to top-level nodes, and forbids inheriting an ancestor's own mark; 別紙C L166 restates it at acceptance level; the L215-221 sample matches. 詳細設計 §19 L1531 grants only 文字種/折返し latitude, bounded by L1526 (\"観測可能な契約を変更しない範囲\") — and the defect is glyph-independent: sibling position is never computed. (d) Code current at HEAD e126bde: L3886 hardcoded \"├─ \", L3950 node line has no mark, L3956 hardcoded item \"├─ \", L3962 appends to the parent's full prefix. `grep -rn '└─' --include=*.rs crates/` = 0 hits.",
   "repro": "Rebuilt from current HEAD (`cargo build -p vtest-cli`, cargo reported fresh against current sources), ran only inside `mktemp -d` (/tmp/tmp.ZdkKrDSG9P = <HOME>\\AppData\\Local\\Temp\\..., outside the repo). (A) bare `init` + `verify`: `├─ scope:PROJECT` for the only/last top-level node, then 12 lines all `├─ │  ├─ <item>`, last one `test_traceability PASS` with `├─ `. (B) SPEC-A/REQ-A/VO-A/VO-B, `verify --req REQ-A`: `cat -A` shows the VO-A (middle) and VO-B (last) lines carry byte-identical prefixes `M-bM-^TM-^\\M-bM-^TM-^@ M-bM-^TM-^B  ` (= `├─ │  `); descendant node lines carry no branch mark; the root's `├─ ` opens all 10 descendant lines; no `└─` anywhere. `report --req REQ-A` produces the same tree. Output matches the dossier's \"actual\" exactly. Temp dir contents were not copied into the repo.",
   "contamination_check": "Not tainted, on two independent legs. (1) The contamination was the repo-root `.verify/config.yaml`. Both the original and my repro ran `vtest init` in a temp project under AppData\\Local\\Temp, which is not a filesystem descendant of <REPO>, so no config walk-up can reach the repo's `.verify/`. Moreover the defect is a compile-time string literal — no config value can introduce a `└─` that does not exist in the source (grep: 0 hits in crates/). (2) I re-established the result independently from a HEAD-fresh binary in a new temp dir, after the restore, with the repo tree clean (`git status --porcelain` empty before and after). The dossier's only soft spot was reusing a prebuilt binary of unverified provenance; rebuilding closed it.",
   "notes": "Citation drift, substance unaffected: the dossier's impl_path says \"L3956 prints the node's own line\" — at HEAD the node line is L3950 and L3956 is the item line. Both facts it asserts are true, just split across the two lines. The dossier itself already corrected the candidate's spec cite (L234-236 -> L232-234); I verified L232-234 is right. Cluster \"text-report-not-annex-a\" names precisely this member's mechanism (hardcoded top-level mark, prefix-append inheritance, absent last-sibling glyph) and its locus lines (3858-3935, 3884-3887, 3937-3965) match the code I read; the vtest-verify item_copies half of the root cause belongs to the VO-AGG members, but the renderer half alone fully produces VO-REPORT-05's defect. Three-member cluster, so NOT_APPLICABLE does not apply. Cluster confidence \"medium\" is, for this member, understated."
  },
  {
   "vo_id": "VO-PLAN-02",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachable: `verify --items spec_coverage` and full verify both hit the arm; crates/vtest-verify/src/lib.rs:546-583 is still the sole producer (grep: only 416/444 copy it into nodes). (b) No earlier gate: hand-written REQ/VO were accepted; verify exited 0 with Result: PASS. (c) Normative text verified verbatim: 別紙C L157/158, 基本仕様 L419/424, 詳細設計 L1333/1348-1352, 別紙A L165. (d) Code unchanged: verify lib.rs:565-568 PASS on `!reqs.is_empty() && missing.is_empty()` with basis \"every selected REQ has at least one linked VO\"; cli/src/lib.rs:922 and mcp:676 `matches!(kind,\"test-semantic\"|\"vo-coverage\"|\"impl-consistency\")`; records.rs:645-652 whitelists ...|\"static\". Two half-hits, both non-load-bearing: point 6 (\"expected\") says MISSING for condition (a), but canon prescribes NOT_CHECKED when no audit ever ran (基本仕様 L425, 詳細設計 L1352) — non-PASS either way, so the 別紙C:157 prohibition still breaks. Point 7 misstates why TEST-CLI-040 is vacuous: fixture tests/fixtures/calc/m1/base/.verify/req/ holds only .gitkeep, so it is non-PASS via \"no REQ records exist\", not via VO-KNOWN requirements: [].",
   "repro": "Independently re-reproduced today, repo read-only, cwd only in mktemp -d (/tmp/tmp.8moAumgQ3l), prebuilt <REPO>/target/debug/vtest.exe (cargo build -p vtest-cli: up-to-date). A: vtest init; docs/spec.md with R1/R2/R3; spec add --id SPEC-X (sha256:32eaad5f..., matching dossier); hand-wrote REQ-A (active, spec_refs SPEC-X/R1) + VO-A (requirements [REQ-A]); .verify/audits holds only .gitkeep. `verify --items spec_coverage` -> spec_coverage PASS, Result: PASS, exit 0; JSON basis [\"every selected REQ has at least one linked VO\"]. `audit bundle --kind spec-coverage --spec SPEC-X` -> \"unexpected argument '--spec' found\"; with --req REQ-A -> NG E-OP-001 unsupported audit bundle kind spec-coverage. B: fresh init, zero SPECs, REQ-B/VO-B -> spec_coverage PASS both in --items scope and in the fixed-12 完全検証 (which is exactly 別紙C L158's forbidden case).",
   "contamination_check": "Not tainted. The contamination was the repo-root .verify/config.yaml overwritten with the calc-m1-base fixture config; `git status --porcelain .verify` is now clean. This dossier's evidence never depended on the repo's own .verify: both repro projects are created by `vtest init` in temp dirs outside the repository, each with its own version-2 config carrying the standard 12-item full_scope (verified by cat). I re-ran both conditions from scratch after restoration and got byte-identical outcomes to the dossier. The one in-repo corroboration (TEST-CLI-077) was read, not executed, and I confirmed its assertions at crates/vtest-cli/tests/m6_acceptance.rs:422-440: full verify, ok == true, spec_coverage == PASS.",
   "notes": "Cluster \"spec-coverage-is-req-vo-linkage\": the stated root cause (REQ->VO linkage evaluator plus an unconstructible spec-coverage audit kind) is exactly what generates this member's PASS, and the locus lines all check out, so attribution HOLDS. Suggested dossier corrections (do not change the verdict): expected value in condition (a) is NOT_CHECKED per 詳細設計 L1352, and TEST-CLI-040's vacuity is due to zero REQ records in the m1 base fixture. Also worth noting for the fix shape: 詳細設計 L1333 homes the item at SPEC, while the implementation attaches it to REQ nodes."
  },
  {
   "vo_id": "VO-EXIST-08",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachability: one unparseable .rs file in a stock `vtest init` project triggers it; reproduced independently. (b) No earlier gate: scan returns exit 1 with a materialized result (matching 別紙C line 88's exit partition), and verify applies no rejection. (c) Spec re-read verbatim: 詳細設計 line 587, line 838 (E-SCAN-001 => batch Incomplete), line 872, line 1378 (「discovery結果が不完全または解析不能なら`UNKNOWN`とし、PASSにしない」), 別紙C line 79. \"Expected = UNKNOWN, never PASS\" is exactly what the normative text requires. (d) Current code matches: discovery.rs:1178 hard-codes `DiscoveryCompleteness::Complete`; vtest-scan/src/lib.rs:98 rejects any non-Complete batch; evaluate_test_traceability (vtest-verify/src/lib.rs:659-685) has no Unknown branch and ignores E-SCAN-001; ScanResult (vtest-scan/src/lib.rs:54-59) has no completeness field.",
   "repro": "Independent, fully isolated (mktemp -d, cwd inside it, absolute path to binary; repo read-only, `git status` clean before and after). `cargo build -p vtest-cli` (up to date at HEAD f5053d6).\n\nD=/tmp/tmp.RwkgaYdHnn; mkdir src; vtest init -> config version 2, adapter rust-cargo, full_scope includes test_traceability (a genuine init config, NOT the calc-m1-base fixture).\nprintf 'fn broken( { this is not valid rust ][\\n' > src/broken.rs\nvtest scan --format json -> EXIT=1, summary {files:0,tests:0,sources:0}, diagnostics [E-SCAN-001 \"failed to parse ... cannot parse string into token stream\"].\nvtest verify --format json -> EXIT=1; test_traceability = PASS, basis \"all 0 discovered managed Test(s) are structurally traceable\"; only FAIL is vo_decomposition \"scan emitted an error diagnostic\" (the catch-all at vtest-verify/src/lib.rs:583-590).\nvtest verify --items test_traceability -> EXIT=0, ok=true, report.result=PASS, E-SCAN-001 still in diagnostics.\n\nByte-for-byte match with the dossier's recorded outputs.",
   "contamination_check": "CLEAN — not an artifact. The contamination overwrote the REPO ROOT's .verify/config.yaml with a calc-m1-base fixture; this dossier's repro never used the repo root. It ran `--project <temp dir>` with a config produced by `vtest init` in that temp dir, and the config it records (version 2, adapter rust-cargo, scan.include=[src,tests,crates], full_scope of 12 items) is exactly what a clean `vtest init` emits — I regenerated it independently and it matched. My re-repro additionally kept cwd inside a fresh mktemp -d and invoked the binary by absolute path, so no repo config could be read. Every observable (exit codes 1/1/0, the 0-test \"complete\" summary, the PASS value and its basis string, E-SCAN-001 text) reproduced identically.",
   "notes": "Two immaterial citation errors, neither touching the conclusion: (1) the dossier says the only `DiscoveryCompleteness::Incomplete` construction is vtest-scan/src/lib.rs:1651, but there is a second at crates/vtest-adapter-api/tests/w1_acceptance_binding.rs:408 — also test-only, so \"no shipped adapter ever sets Incomplete\" still holds. (2) \"ScanResult ... crates/vtest-scan/src/lib.rs:84-90,143\" points at MaterializedDiscovery; ScanResult is at lines 54-59. Both genuinely lack a completeness field, so the structural-unavailability claim stands.\n\nSingleton, so cluster attribution is N/A, but the stated singleton rationale is accurate on every mechanical detail I checked. Also note the dossier's own aside is confirmed: vtest-verify/src/lib.rs:583-590 maps any scan error to vo_decomposition FAIL, which 詳細設計 line 1371 explicitly forbids for adapter source-parse errors."
  },
  {
   "vo_id": "VO-INTAKE-08",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (d) Code still says what the dossier claims: vtest-verify/src/lib.rs:584-598 is verbatim the any-error / empty-vo_ids / else-PASS arm; vtest-scan/src/lib.rs:714-767 and 769-845 validate id/readability/fields/status/coverage_policy/dimensions and never touch spec_refs; `grep -rn E-SCAN-012 --include=*.rs crates/` = 0 hits. (c) Spec re-read: 詳細設計 §11.1.1 line ~1365 says spec_refs.spec 解決不能 → MISSING, PASS only if spec_refs SPEC references resolve; line 849 defines E-SCAN-012; line 1371 names it as one of four codes moving this item. Expected is quoted, not inferred. (a) Reachable: pure CLI records + `rm .verify/spec/SPEC-REAL.yaml`. (b) No earlier gate: spec_refs_from_args (vtest-cli/src/lib.rs:3646-3670) guards only `add`/`edit` args; approval.rs:93-158 gates approval only, and vo_decomposition is computed without it.",
   "repro": "Independently re-reproduced. Built `cargo build -p vtest-cli` (repo, target/ only); ran in a fresh `mktemp -d` (/tmp/tmp.yuajzvRACa), never in the repo, nothing written inside the repo. init → spec add SPEC-REAL → req add REQ-ADD --spec SPEC-REAL → vo add VO-ADD --req REQ-ADD --spec SPEC-REAL (persisted spec_refs [{SPEC-REAL,\"1. contract\"}]). Baseline: vo_decomposition PASS. Then `rm .verify/spec/SPEC-REAL.yaml` (dir left with only .gitkeep). scan --format json → diagnostics = [W-SCAN-102 warning] only, no error, no E-SCAN-012. verify --items vo_decomposition → vo_decomposition PASS, basis \"scan and canonical VO records are structurally readable\", result PASS; `--quiet` exit 0. Full verify → spec_coverage PASS, vo_decomposition PASS, test_existence MISSING (unrelated, no test), result MISSING. Matches the dossier's Construction A line for line. Construction B not needed.",
   "contamination_check": "Not tainted. (1) Structural isolation: project root is resolved by cwd ancestor-walk for a `.verify` dir (vtest-store/src/lib.rs:534-546). The original repro ran under AppData\\Local\\Temp, a different tree from <REPO>, so the repo-root `.verify/config.yaml` overwrite could never be reached; there is no home/global/env config fallback. (2) The contamination was a config data file, not source, so a prebuilt binary was unaffected. (3) Decisive: I re-established the result independently today in a fresh mktemp -d with a freshly built binary, and got byte-identical behavior (PASS + no error diagnostic + exit 0). Repo verified clean afterwards (`git status --porcelain` empty).",
   "notes": "Chain points 1-8 all survive; none is factually wrong. Cluster attribution HOLDS: the cluster's root cause (no component resolves the spec_refs axis; E-SCAN-012 has no emit site; vo_decomposition is a blanket any-error placeholder) is exactly the mechanism I observed producing this member's defect, and every locus line (vtest-scan 713-766/768-841, vtest-verify 584-598, vtest-cli 3646-3670) matches current code within off-by-one drift. Side observation, belonging to a different dossier, not to this one: spec_coverage also stayed PASS with the SPEC record deleted, which independently contradicts 詳細設計 §11.1's spec_coverage rule (\"SPEC recordまたはsourceが存在しなければMISSING\"). Minor housekeeping: the temp repro dir could not be deleted (permission denied on rm); it is outside the repo and harmless."
  },
  {
   "vo_id": "VO-PLAN-04",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachable: I re-ran the full path end to end. (b) No earlier gate: bundle/kind/hash/verdict/reasons checks (lib.rs:1125,1132,1853-1950,1953-1975) all pass; validate_reasons is the last gate. (c) Spec re-read confirms point 6: 基本仕様 7.4 lines 431-438 lists SPEC ID+section, viewpoint, exclusions+grounds as required (only examples 必要に応じて) and states verbatim that a COMPLETE submission lacking the reason structure is not accepted and vo_coverage stays 非PASS; 要件定義 §6 (332-369) and 詳細設計 8.3 (1125-1140) re-bind vo-coverage to it. The best counter — 詳細設計 8.4 check 6 (1163-1164) matches the impl exactly — only shows 8.4 under-enumerates against 8.3 in the same document; it excuses the missing error code, not the acceptance. (d) Code still says it: lib.rs:2022-2043 checks only viewpoint-kind + spec-kind presence, no ref format/resolution; 2113-2140 synthesizes exclusions: []; vtest-verify lib.rs:1265-1353 ignores reasons. The exclusions leg is partly contestable (vacuous if nothing excluded), but leg (b) — ref \"trust me bro\" accepted as 仕様箇所 — carries the chain alone.",
   "repro": "Fresh dir outside repo (mktemp -d → <HOME>\\AppData\\Local\\Temp\\tmp.NjjnvcFvfD\\proj); binary target/debug/vtest.exe from `cargo build -p vtest-cli` (exit 0, writes only target/). No file created or modified inside the repository. Steps: init; spec add SPEC-REPRO --path spec.md; req add REQ-REPRO --spec SPEC-REPRO --sections 3.1; vo add VO-REPRO; audit bundle --kind vo-coverage → 01M02VPRWK70085R0002CAG000; submit res.json (verdict COMPLETE, one decomposition-viewpoint reason, basis {kind: spec, ref: \"trust me bro\"}, NO exclusions key) → exit 0, accepted, audit 01M02VPZ5R2SVEAK00023GR000, verdict PASS; vo approve; re-bundle 01M02VQ5NT086M4T00038M0000; re-submit → accepted 01M02VQC1641E11D00023D8000; verify --items vo_coverage → vo_coverage = PASS. Persisted .verify/audits/01M02VQC…yaml carries ref 'trust me bro' plus a synthesized `exclusions: []`. Matches the dossier's \"actual\" in every element.",
   "contamination_check": "Not tainted. find_project_root (lib.rs:3697) walks up from the given --project path only, and VerifyLayout derives config/audits from that root (vtest-store/src/lib.rs:45-70). Both the original repro (scratchpad under %TEMP%) and mine (<HOME>\\AppData\\Local\\Temp\\tmp.NjjnvcFvfD) sit outside the repo, so the repo's .verify/config.yaml is unreachable from either — the calc-m1-base overwrite could not have influenced them. I re-reproduced from a clean init anyway, on a clean tree (git status empty, HEAD e126bde), and every observed value matches the dossier. The conclusion also does not rest on the repro alone: the code path is verified statically at crates/vtest-cli/src/lib.rs:2022-2043 and crates/vtest-verify/src/lib.rs:1265-1353.",
   "notes": "Key file:line — crates/vtest-cli/src/lib.rs:1977-2046 (validate_reasons; vo-coverage branch 2022-2043), 2113-2140 (audit_record_yaml synthesizes `exclusions: []`), crates/vtest-store/src/records.rs:605 (record parser REQUIRES exclusions, which is why the CLI fabricates it), crates/vtest-verify/src/lib.rs:1265-1353. E-AUDIT-007 has zero occurrences in crates/ (only E-AUDIT-001..006 exist). Singleton, so cluster attribution is NOT_APPLICABLE; the stated singleton rationale is otherwise accurate, except that its E-AUDIT-007 clause names the spec-coverage defect the dossier itself labels \"adjacent only, not part of this chain\" — a locus mismatch in the cluster note, not in the dossier. Residual soft spot for the fix owner: 詳細設計 8.4's checklist must be re-synced with 8.3/基本仕様 7.4 (missing exclusions and SPEC-ID#section format checks for vo-coverage), otherwise a fix has no error code to emit."
  },
  {
   "vo_id": "VO-APPROVE-07",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachable: validate_id (lib.rs:3745) checks prefix/charset only; approver and basis gates pass; the repro lands on E-CORE-001 directly. (b) No earlier gate: bogus approver correctly gives E-OP-001/exit 2, so nothing swallows the target case. (c) Expected is right: 別紙A L104 names 対象 alongside 依存entity under one predicate and states 終了コード2; 詳細設計 L1496 and 別紙C L85 agree; §17.2 puts E-APPROVAL-* in class 2; E-CORE-001 occurs in no normative doc. §17.2's \"3 over 2\" precedence is a multi-candidate tiebreak, not a reclassification — reading missing-target as tool-internal makes L104's 対象 clause dead text, and the impl's own dependency path calls the identical fact exit-2. The E-OP-001 alternate reading also demands exit 2. (d) Code current: lib.rs:3400 read_vo(...).map_err(store_failure)?, store_failure = E-CORE-001/ExitCode::Internal (3791-3793), Internal=3; the E-APPROVAL-001/Usage wrap sits at 3403-3408, unreachable. Link 1's claim matches dogfood-ontology-topdown-v2.json verbatim.",
   "repro": "Independently re-reproduced, read-only on the repo. cargo build -p vtest-cli reported \"Finished\" with nothing to rebuild, so target/debug/vtest.exe equals current source. Fresh mktemp -d outside the repo (<HOME>\\AppData\\Local\\Temp\\tmp.oj4akiMQDa), `vtest init` there (its own generated config.yaml). [1] `vo approve VO-NOEXIST --approver-kind human --approver-id reviewer` -> \"error E-CORE-001: I/O error at ...\\.verify\\vo\\VO-NOEXIST.yaml ... (os error 2)\", exit 3; JSON output identical; .verify/approvals holds only .gitkeep. [2] control `--approver-kind bogus` -> E-OP-001, exit 2. [3] asymmetry: `vo add --id VO-CHILD`, rewrite parent to 'VO-GONE', approve -> \"E-APPROVAL-001: cannot approve VO-CHILD: upstream vo VO-GONE is not completely and currently resolvable\", exit 2. Identical missing-file fact: exit 2 as dependency, exit 3 as target. Nothing written inside the repo.",
   "contamination_check": "Not tainted, on two independent grounds. First, exposure was unlikely: this dossier's repro ran `vtest init` in its own scratchpad project, so it never consumed a .verify/config.yaml from the repo root; the calc-m1-base fixture copy appears in VO-EXIST-05's repro, not this one. Second, and decisive: I re-established all three observations from scratch in a fresh temp project outside the repo, with a config generated by the binary itself and cwd never inside the repo, so no repo-root config could participate. The observed values (E-CORE-001, exit 3, empty approvals; E-OP-001/2; E-APPROVAL-001/2) match the dossier exactly, and the code path producing them is config-independent — it is a plain file-read failure mapped by store_failure.",
   "notes": "Cluster attribution is NOT_APPLICABLE (singleton), but its stated rationale is accurate: read_vo(...)? at lib.rs:3400 returns through store_failure before the E-APPROVAL-001/exit-2 wrap at 3403-3408 — a pure error-mapping defect, confirmed verbatim in current code. Two immaterial nits, neither a chain link: (1) the dossier cites 詳細設計 §6.2 for E-OP-001 structured-operation validation; §6.2 is actually \"rust-cargo locator解決\" — but the dossier is mirroring the spec's own table cross-reference at L1498, and the citation sits inside an explicitly rejected alternate reading. (2) The scope wording \"missing/unreadable\"—only the missing sub-branch was reproduced; a permission-denied read is a weaker case, but missing alone suffices. Confirmed no normative doc defines E-CORE-001 (grep over docs/*.md is empty)."
  },
  {
   "vo_id": "VO-STATAUDIT-04",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachable: `scan` accepted the input with zero diagnostics (files 2/sources 4/tests 2); the shape is the repo's own CLI-suite idiom. (b) No earlier gate: DA-006 is the SOLE FAIL; DA-001/004/005 PASS, DA-002/003 UNKNOWN, so absent DA-006 the record would be UNKNOWN. (c) Spec re-read in full: REQ §11 l.507 catch-all is semantic (検証結果が対象実装の正しさに依存しない構造) and l.509 authorizes pre-LLM rejection only for 明確にNG; 基本仕様 §7.2 l.370-371 and 別紙C §18.3.2 both mandate UNKNOWN for analysis limits, and 別紙C binds DA-001..006 to 本冊 §7. Only the downstream 詳細設計 §7.2 DA-006 row (\"退避例: なし\") dissents, and it cannot override its upstream. Mutation (a+b→a-b) made the helper test FAIL, empirically falsifying the catch-all's antecedent. (d) Code current: citations confirmed.",
   "repro": "Fully re-reproduced independently. Fresh `mktemp -d` outside the repo: <HOME>\\AppData\\Local\\Temp\\tmp.fzoHORLRVR (repo never written; only target/ via `cargo build -p vtest-cli`, which reported Finished with no recompile = binary already at HEAD). Hand-wrote Cargo.toml/src/lib.rs (add)/tests/registered.rs (TEST-HELPER-ASSERT via assert_sum_is helper; TEST-INLINE-ASSERT control) + .verify/vo/VO-KNOWN.yaml after `vtest --project <P> init` generated a clean project-local config. Results, identical to the dossier: scan OK {\"files\":2,\"sources\":4,\"tests\":2}, no diagnostics; `audit static` -> TEST-HELPER-ASSERT verdict FAIL with DA-006 FAIL \"no assert-like syntax is present\" as sole FAIL, control verdict UNKNOWN with DA-006 PASS; `audit bundle --kind test-semantic` -> helper test {\"skipped\":true,\"reason\":\"static audit verdict is FAIL...\"} vs control bundled (01M02VRMMW0ZH62Y0003KQ8001). Mutation: 2 passed -> 2 failed (helper_delegated_assert, inline_assert); lib.rs restored.",
   "contamination_check": "Not tainted. The repo's .verify/config.yaml is currently the correct SpecTracer config (name: SpecTracer, include: crates), i.e. restored. My repro never touched the repo: cwd was the temp dir, --project pointed at the temp dir, and `vtest init` generated that project's own config there. More decisively, the defect is config-independent by construction: is_assertion_macro (static_audit.rs:1326) matches syn::Macro nodes only, so no assertion_macros value in any config — contaminated or not — can make a helper ExprCall count. The outcome is therefore not an artifact of the earlier calc-m1-base contamination, and I re-established every reported observation from scratch.",
   "notes": "Code re-verified at HEAD: crates/vtest-adapter-rust/src/static_audit.rs:1190 has_assert_like visits only the test's own ItemFn (collect_assertions :1296, VerificationSyntaxVisitor :1201+, returns_result_and_can_err) with no callee expansion; :1778 call site; :1800-1812 DA-006 RuleResult verdict = if has_assert { Pass } else { Fail } — UNKNOWN unreachable. Two nits, neither overturning: (1) the dossier cites the call site as :1777, actual is :1778 (off-by-one; the :1800-1812 range is exact). (2) The dossier's negation point 6 concedes the impl faithfully implements 詳細設計 §7.2's DA-006 row and that the fix direction is spec-side, yet the entry's \"spec_gap\" field is left empty — that row (l.985) should be recorded as the spec-side locus. Cluster: singleton; its stated rationale (\"DA-006's verdict is Pass/Fail with no UNKNOWN branch, so an analysis-scope limit is reported as a confirmed violation\") is verbatim accurate against the code and does produce this defect, hence HOLDS rather than NOT_APPLICABLE. Not UNCLEAR: the layer hierarchy (REQ/基本仕様/別紙C vs 詳細設計) resolves the ambiguity, and the mutation test settles the semantic predicate empirically."
  },
  {
   "vo_id": "VO-EXIST-05",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (c) Normative source is verbatim-exact: 基本仕様 §7.4 「active REQに対応VOが1件もなければ`vo_coverage = MISSING`」 and 詳細設計 §11.1 「`vo_coverage`はactive REQごとに対応VO集合を評価する。対応VOが0件なら`MISSING`」, with the 0-VO clause ordered before the audit clause. §4.2 row 3 defines vo_coverage as \"VO群が対応REQを網羅しているか\"; §4.3 priority puts MISSING above PASS, so the per-REQ MISSING must dominate. (d) Current code matches: dispatch lib.rs:599; evaluate_vo_coverage lib.rs:1265; early NotChecked at 1271-1276; folds only over selection.vo_ids; req_ids (declared :164, populated :189) is never read in it. (a) Reachable — active REQ with no VO is the state of any freshly added REQ; diagnostics empty. (b) No earlier gate: in `--items vo_coverage` every other item reports \"outside requested scope\". Only flaw found is a parenthetical mis-citation of 別紙C:157 (it forbids PASSing spec_coverage on REQ→VO existence, not spec_coverage reporting it at all) — a labeled side remark carrying no chain point.",
   "repro": "Independently re-reproduced. Fresh `cargo build -p vtest-cli`; `mktemp -d` → /tmp/tmp.Z0dxeebBfL (outside repo); copied tests/fixtures/calc/m1/base; added REQ-A.yaml (status: active, referenced by no VO) and REQ-B.yaml; set VO-KNOWN.requirements=[REQ-B]; mkdir .verify/approvals. Bundle+submit vo-coverage COMPLETE (01M02VQJAH7AZ4BA0003RRR000) → vo approve VO-KNOWN → re-bundle+re-submit (01M02VR05R185KAK0002AYR000). Results, all cwd inside the temp dir: (A) `verify --items vo_coverage --summary` → ok=true, result=PASS, non_pass_items=0, EXIT=0. (B) full `verify` → ok=false, vo_coverage=PASS [\"VO VO-KNOWN: Pass (coverage audits: ...)\"], spec_coverage=MISSING [\"selected REQ record(s) have no linked VO: REQ-A\"]. (C) `verify --req REQ-A` → vo_coverage=NOT_CHECKED [\"no VO records are available in the selected scope\"]. Diagnostics empty everywhere. MISSING never produced. Repo untouched (read-only).",
   "contamination_check": "Not tainted. Two independent reasons. (1) Structural: run_verify resolves the project root from `--project` (crates/vtest-cli/src/lib.rs:2464 resolve_root → find_project_root(absolute_path(project))) and loads config from that root (:2468); the temp project ships its own .verify/config.yaml, so a contaminated repo-root config cannot leak into the run. Additionally the contamination *was* the calc-m1-base fixture config, i.e. byte-identical to what this repro legitimately uses. (2) Empirical: I re-ran all three observations from scratch in a new mktemp -d project with a freshly built binary and got the identical values, basis strings and exit codes. Note the fixture config is version 1 with an 11-item full_scope (no test_traceability), normalized in-memory to the fixed 12 — irrelevant to runs (A) and (C), and run (B)'s vo_coverage value is unaffected.",
   "notes": "Cluster \"vo-coverage-not-req-anchored\" (members VO-EXIST-05, VO-PLAN-07): its stated root cause — vo_coverage implemented per-VO instead of REQ-anchored, req_ids never read, early NOT_CHECKED on empty VO set — is precisely the mechanism that produces this member's defect, so HOLDS. One imprecision in the cluster text: \"MISSING is unreachable\" is overstated — MISSING is reachable at lib.rs:1331-1332 (valid PASS audit + unapproved VO); it is unreachable only for the zero-VO-REQ cause. The locus line numbers drift by 1-4 (evaluate_vo_coverage is 1265, early return 1271; build_req_node 402, build_vo_node 448) but point at the right code. No test in the repo asserts vo_coverage=MISSING anywhere (grep over crates/**/tests), consistent with an unmitigated gap. Fix shape proposed by the cluster is sound."
  },
  {
   "vo_id": "VO-SEMAUDIT-02",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachable: discovery.rs:1030 rejects >1 target only when kind lacks \"integration\"; my scan of a 2-target integration Test returned ok:true, 0 diagnostics. (b) No later gate: audit submit (lib.rs:1104-1133) runs only kind-match, validate_bundle_subjects (1853-1933, loops present subjects only), verdict and reasons checks; audit_record_yaml (~2057-2063) copies bundle subjects verbatim. (c) Spec re-read confirms \"expected\": 詳細設計 §8.1 L1052 (全targetのimplementation construct source全文), §8.4 L1170 (test-semanticではTest subject・VO・全target), §8.5 L1180 (有効 = 対象集合が現在の監査対象集合と完全一致), 基本仕様 §7.3 L403-405. (d) Code still matches: lib.rs:1215-1225 test.targets.first(), 1237-1242 one target subject, :1291 singular \"target\"; vtest-verify lib.rs:1385-1388 valid = subjects.iter().all(subject_is_current). Half-landed only: §8.2's plural \"targets\" is labeled 例, so the key name is non-normative — but the omitted source/subject is mandated by §8.1/§8.4 regardless.",
   "repro": "Independent re-repro, fully isolated. Rebuilt binary (cargo build -p vtest-cli, repo clean, writes only target/); fresh mktemp -d project /tmp/tmp.eNGK8QFVrp assembled from tests/fixtures/calc/m1/base (config.yaml, VO-KNOWN, Cargo.toml, registered.rs) + src/lib.rs with known/also_known + TEST-M1-MULTI (@vtest.kind integration, two @vtest.target); git init/commit; every vtest run cwd inside temp with --project absolute temp path. [1] scan ok, 0 diagnostics, both targets, hashes e757cc…/89af7d… identical to dossier. [2] bundle ok, no diagnostics. [3] bundle has no \"targets\" key; singular \"target\" = src/lib.rs::known; subjects = exactly 3 (test, one target, vo); also_known absent silently. [4] submit accepted, record subjects hold one target locator. [5] verify: \"Test TEST-M1-MULTI: Pass\". [6] mutate also_known → still \"Pass\", not stale. Added CONTROL not in the dossier: mutating the recorded target known instead → \"Stale (…(stale))\", value STALE. Staleness machinery is live; only the dropped target is outside its domain.",
   "contamination_check": "Not a factor, and independently re-established. The contamination was repo-root .verify/config.yaml; the dossier's repro and mine both ran with --project pointing at a temp project outside the repo, and my config.yaml was copied from the tracked fixture (repo git status clean, porcelain empty). The one genuinely stale input in the original was the prebuilt binary (\"used as-is, no rebuild\"); I rebuilt from current HEAD and got byte-identical observables (same content hashes, same 3-subject list, same verify basis strings). Repo remained read-only throughout: no vtest run and no file write inside <REPO>; only target/ was touched by cargo build. Not TAINTED.",
   "notes": "Chain points 1-8 all stand; nothing overturned. UNCLEAR explicitly rejected: §8.5's per-kind enumerations name only static and impl-consistency, but its general predicate (対象集合が現在の監査対象集合と完全一致) plus §8.4's explicit \"test-semanticではTest subject・VO・全target\" plus 基本仕様 §7.3 L403-405 close the gap; the bundle-content violation (§8.1 全target source) needs no validity argument at all. Cluster attribution HOLDS: this member's defect is exactly the cluster's two named halves — incomplete required-set derivation (test.targets.first(), lib.rs:1215-1225) and snapshot-only validity (validate_bundle_subjects 1853-1933; vtest-verify 1385-1388, subject_is_current 1559-1596) — both verified at the cluster's cited loci. The cluster's fix_shape (one required-subject-set derivation shared by bundle generation and validity) would resolve this member."
  },
  {
   "vo_id": "VO-EXEC-09",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachability: unsigned YAML under .verify/evidence; the project's own acceptance tests TEST-CLI-033/034/035 (crates/vtest-cli/tests/adapter_acceptance.rs:1089-1183) hand-edit exactly this fixture's Evidence after `run --all --fast` and assert STALE/UNKNOWN — same method as my repro. records.rs:1151-1160 `filter_map` drops entries lacking `target:` (no tampering needed; variant A' reproduced). (b) No earlier gate: evidence_record_validity (vtest-verify/src/lib.rs:1765-1820) inspects adapter/execution_state/hashes only; no E-SCAN-010 path exists for Evidence target_execution (grep: scan emits it only for SPEC/REQ/VO/approvals); evidence_validity measured PASS in every variant. (c) Expected: 基本仕様:501 「扱わない」 and 詳細設計:1342 (§11.1 value = §10.2 aggregate of entries) are evaluation-side, killing the \"保存しない is writer-only\" reading; also 詳細設計:487, 1312, 別紙C:173-174. (d) Code unchanged: lib.rs:1667-1674 returns the stored scalar. Extra: variant C (entry FAIL/count 0 + stored PASS) also PASSes, violating 詳細設計:487's aggregate/entry-contradiction clause.",
   "repro": "Independent, isolated. Fixture tests/fixtures/calc/m1/base copied to scratchpad/revo9 (never the repo; repo read-only, git status clean). Fresh git init/commit 566c548, binary <REPO>/target/debug/vtest.exe built from current HEAD (cargo build -p vtest-cli, up to date). `vtest run --all --fast` -> Evidence 01M02VPQYK79DKDB0000JXG000, hashes.targets=[rust-cargo::src/lib.rs::known]. Controlled single-block delta (only target_execution edited), each state probed for target_execution and evidence_validity (scratchpad/step3.py, step3.out):\n- BASELINE checked:false -> NOT_CHECKED, exit 1\n- A empty targets, stored PASS -> PASS, exit 0\n- A' legacy entry lacking `target:` -> PASS, exit 0\n- B duplicate+extra+undeclared locator -> PASS, exit 0\n- C entry FAIL/count 0, stored PASS -> PASS, exit 0\n- RESTORE baseline -> NOT_CHECKED, exit 1\nevidence_validity = PASS in all six.",
   "contamination_check": "Not tainted, by mechanism and by re-establishment. The original repro ran with cwd inside a temp copy carrying the fixture's own .verify/config.yaml, so an overwritten repo-root config had no path into it; the repo config is verified restored (project: SpecTracer, scan.include: crates, run.coverage: llvm-cov) and differs from the calc-m1-base fixture config. More decisively, the verdict does not rest on the original repro at all: I re-derived every observation from a fresh fixture copy, fresh git history, fresh `vtest run`, and a controlled baseline -> variant -> restored-baseline sequence, and the NOT_CHECKED/PASS/NOT_CHECKED bracket proves the PASS is caused solely by the target_execution block edit, not by any ambient config.",
   "notes": "Cluster attribution HOLDS: the stated root cause (evaluate_target_execution returns record.target_execution.result and never reads .targets) is the direct and sole producer of this false PASS; locus lines are current (vtest-verify/src/lib.rs:1648-1684, evidence_basis :1753-1763, test_node :489-503 with children = Vec::new()). Suggested dossier strengthening: variant C (aggregate/per-target contradiction) is a fifth counterexample the dossier omits, and 詳細設計 §3.7:487 「aggregate resultとtarget別結果の矛盾は E-SCAN-010」 is a reader-side obligation not cited in the dossier's normative_source — it makes the \"writer-side only\" counter-reading untenable on its own. No file in the repository was created or modified; all repro artifacts live in the scratchpad."
  },
  {
   "vo_id": "VO-STRUCTOP-09",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachability: the state is a hand-written .verify/forms/py-unit.yaml; load_form_schema (vtest-store/src/forms.rs:119-148) accepts any safe kind and only falls back to built-ins when the file is absent. I reproduced it end-to-end. (b) No earlier gate: the only rejection I hit was my own malformed answers file; with valid answers, validate_form_answers, vo-exists, unique-fn-name, rust-file and the Rust re-parse all pass — none reads FormSchema.adapter. (c) Spec re-read verbatim: 基本仕様 §8.5, 別紙A §14.2/§14.3/§14.4, 別紙C §18.3.7 all require rejection with no file change; 基本仕様 §11.1 fixes exit 2. Quotations are accurate, no competing reading. (d) Code still says it: operations.rs:12 static rust import, create_test takes no registry, structured_test/built_in_form_kinds/accepts_compatibility_form have zero call sites, rust_cargo_registration (adapter-rust:88-103) omits StructuredTest.",
   "repro": "Independent, isolated, fresh build. `cargo build -p vtest-cli` at HEAD e126bde; `mktemp -d` -> /tmp/tmp.EQGDcby5gD (<HOME>\\AppData\\Local\\Temp\\tmp.EQGDcby5gD), all commands run with cwd inside it. Repo never written.\n\nSetup: Cargo.toml (structop-fixture) + src/lib.rs `pub fn add`; `vtest init`; `vtest vo add --id VO-CALC-ADD`; py-unit.yaml = built-in rust-unit-function with kind: py-unit / adapter: python-pytest (unregistered); answers.yaml with nested `answers:` block.\n\n`vtest test create --form py-unit --answers answers.yaml` -> OK, EXIT=0, data {\"changed\":true,\"file\":\"src/lib.rs\",\"test_id\":\"TEST-CALC-001\",\"start_byte\":63,\"end_byte\":339}; src/lib.rs mutated on disk with the rendered #[test].\n`vtest scan` -> EXIT=0, {\"files\":1,\"sources\":2,\"tests\":1}.\n`vtest test list --format json` -> execution.adapter=\"rust-cargo\", location.adapter=\"rust-cargo\".\n\nMatches the dossier's points 7 and the forbidden rust-cargo fallback exactly.",
   "contamination_check": "Not tainted. The contamination was the repo-root .verify/config.yaml. This dossier's path never reads it: create_test calls load_config(root) where root is the target project, and the original repro used `--project D` against a temp project with its own vtest init-created .verify/. My re-repro removes the question entirely — fresh mktemp -d project, its own `vtest init` config, cwd inside the temp dir, no --project flag, and a binary rebuilt from current HEAD rather than the dossier's prebuilt one. The defect reproduced identically, so the conclusion does not depend on any contaminated context.",
   "notes": "One notes-level over-claim in point 7 (actual): \"No E-ADAPTER-004 can be emitted anywhere in the system because its only source has zero call sites.\" AdapterError::MissingCapability (adapter-api:244) is constructed by missing() at :628, which other capability accessors reach — called at vtest-cli/src/lib.rs:326/348/761/1194/2306 and vtest-scan/src/lib.rs:1543. So the code is emittable in principle for other capabilities; it is unreachable for StructuredTest specifically, which is the operative claim. Wording refinement only; no chain point flips.\n\nCluster: I verified every locus line (operations.rs:12/:52/:162, adapter-api:438-441/:596-604, forms.rs:119-193, adapter-rust:88-103). The stated root cause — no Form-to-adapter resolution step exists in create_test/edit_test — is precisely the mechanism producing this member's defect."
  },
  {
   "vo_id": "VO-EXIST-09",
   "verdict": "UPHELD",
   "cluster_attribution": "DOES_NOT_HOLD",
   "refutation_summary": "All four attacks failed. (a) Reachable: reproduced from a hand-written project; scan exits 0 with W-SCAN-101, verify runs to completion (exit 1). (b) No earlier gate rejects; `report` shares the same run_verify/emit path (vtest-cli/src/lib.rs:377-383). (c) Spec verified verbatim in docs/…別紙A…v0.1.md:225-227 plus its normative text example with a `Project checks:` block (line 202) — the \"expected\" is exactly what the spec demands. (d) Current code matches: vtest-verify/src/lib.rs:659-677 flattens diagnostics to `diagnostic.message.clone()` (664, 673); vtest-cli/src/lib.rs:3858-3933 emit_text renders tree XOR items and never prints `basis`; :3937-3964 prints item/value only; `test_traceability` appears in verify at lines 36/654 only, so it is in zero tree nodes; grep finds no \"Project checks\" anywhere in crates/. No crate commits since the dossier commit c493432.",
   "repro": "Isolated: `mktemp -d` → <HOME>\\AppData\\Local\\Temp\\tmp.XgLblH6XNP (outside repo; nothing written in repo). Hand-wrote Cargo.toml, src/lib.rs, tests/registered.rs (@vtest.id TEST-M1-CLEAN / covers VO-KNOWN / target src/lib.rs::known), tests/unregistered.rs (`#[test] fn unregistered_fixture_test() {}`); copied the calc/m1/base .verify/config.yaml + VO-KNOWN.yaml. `cargo build -p vtest-cli` (up to date). scan → OK, warning W-SCAN-101. `verify --items test_traceability --format text` → exit 1: tree of VO/Test only, `Result: MISSING`, one `warning W-SCAN-101: …` line; no test_traceability line, no `Project checks:`, no adapter ID, no source location, no per-test verdict. Same for full 12-item verify and for `report --format text`. `--format json` → items[] has test_traceability MISSING basis=[message only]; diagnostics carry code + location{adapter rust-cargo, path tests/unregistered.rs, locator, byte_range}; tree contains test_traceability nowhere.",
   "contamination_check": "Not tainted, for two independent reasons. (1) Structural: root resolution is `find_project_root` (crates/vtest-store/src/lib.rs:534-546), which walks ancestors of `--project` only; a temp-dir project cannot reach the repo root, so a contaminated repo-root .verify/config.yaml could not have influenced the original repro. Nor is the defect config-sensitive — it is a renderer/scope structure fact. (2) Empirical: I re-established the evidence from scratch in a fresh mktemp dir with a hand-written project (not a copy of any repo state) against a freshly verified build, and got byte-equivalent behaviour. Repo root also verified restored (.verify/config.yaml → `name: SpecTracer`) and the working tree clean apart from untracked docs/plans, docs/reports.",
   "notes": "Cluster rationale: apply the cluster's own fix_shape (structured grounding in ReportItem.basis) and this member's contradiction survives untouched — emit_text still never prints `basis`, and never prints items[] when a tree exists, while test_traceability is in no tree node; the text channel still shows nothing. Conversely a renderer joining envelope.diagnostics could satisfy the spec tuple without touching basis. Neither necessary nor sufficient ⇒ does not produce this defect. The flattening at :664/:673 is real code and a genuine secondary contributor to the JSON-basis half only; the dominant cause is renderer/scope-side (no Project checks section, tree-XOR-items, item absent from the tree model). Minor citation drift in the dossier (663 vs 664; emit_text 3854 vs 3858) — cited code exists and behaves as claimed; not an overturned link. \"No renderer can recover them from basis\" is literally true, though the data is recoverable from envelope.diagnostics."
  },
  {
   "vo_id": "VO-INTAKE-04",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachable: bundle/submit never inspect scan diagnostics (vtest-cli/src/lib.rs:876-957, :1010-1163); both exited 0 with W-SCAN-104 live. (b) No earlier gate: validate_bundle_subjects (lib.rs:1906-1920) re-reads the CURRENT source for kind==\"spec\", so submit accepts. (c) Attack on \"expected\" failed hardest — 詳細設計 §8.5:1179-1180 puts the rule inside the validity definition itself (\"SPEC は登録された sha256 と実ファイルの一致も要求…当該レコードは STALE\"), and :1185 names impl-consistency and the exact violated clause (限定scopeの impl_consistency = PASS へ利用しない). That forecloses the event-anchored reading and UNCLEAR; the spec is more explicit than the dossier cites. (d) Code current at HEAD e126bde: lib.rs:1327-1331 comment intact, verify subject_is_current spec arm (vtest-verify/src/lib.rs:1583-1589) hashes the current file; `sha256` has zero occurrences in vtest-verify/src/lib.rs. Chain point 1 matches the VO claim verbatim (dogfood-ontology-topdown-v2.json).",
   "repro": "Independent, isolated: fresh mktemp -d (/tmp/tmp.6E5FRk4zt5/p, outside repo), vtest rebuilt via `cargo build -p vtest-cli`, project seeded by copying tests/fixtures/calc/m1/base files. spec add SPEC-X (sha256:819bedf3…) → req add REQ-X → VO-KNOWN.requirements=[REQ-X] → scan clean. Drift docs/spec.md BEFORE any audit → scan [W-SCAN-104 warning]. `audit bundle --kind impl-consistency --test TEST-M1-CLEAN` exit 0; cached bundle subject {kind:spec,id:SPEC-X,hash:sha256:507735…} = the DRIFTED source. `audit submit` accepted (audit 01M02VR6JP5D3EQ100023KR000). `verify` → impl_consistency = PASS, basis \"Test TEST-M1-CLEAN: Pass (audits: 01M02VR6…)\" with no \"(stale)\", W-SCAN-104 still emitted. Control A (drift after audit) → STALE \"(stale)\". Control B (spec add --update to the drifted body) → same audit PASS again. Anchor confirmed: \"source unchanged since the audit\", not \"source == registered sha256\". Not re-run: dossier part B (vo-coverage surface); not load-bearing.",
   "contamination_check": "Not tainted, affirmatively. The dossier's repro ran in scratchpad projects outside the repo root, so the repo-root .verify/config.yaml overwrite could not have shaped it; more importantly the verdict does not rest on that evidence at all. I re-established every decisive observation from scratch: new temp directory outside the repository, binary rebuilt from current HEAD, project seeded from the fixture, and I obtained the identical drifted subject hash sha256:507735…, identical registered sha256:819bedf3…, identical PASS-without-\"(stale)\" while W-SCAN-104 was live, and identical Control A (STALE) / Control B (PASS) outcomes. Nothing inside the repository was written or executed; repo access was read-only plus `cargo build` into target/.",
   "notes": "Singleton, so cluster_attribution is NOT_APPLICABLE, but the stated rationale was checked and is accurate on both halves: the spec audit subject binds the current Specification source (vtest-cli/src/lib.rs:1326-1345, :1459-1490) while the approval closure fails closed on the registered sha256 (vtest-store/src/approval.rs:145-152, \"recorded sha256 does not match the current Specification source\"). Two anchors for two sentences of one §11.4 paragraph — exactly the singleton's stated mechanism. One improvement for the dossier: its normative_source spends effort defending a state-anchored reading of §11.4 by inference when §8.5:1179-1180/:1185 states it directly and specifically for impl-consistency; citing §8.5 would make the chain shorter and unassailable. Note also the older docs/plans/dogfood-ontology-topdown.json carries a different VO-INTAKE-04 claim; the dossier correctly uses the v2 claim."
  },
  {
   "vo_id": "VO-SEMAUDIT-07",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (c) Spec quotes are verbatim at three layers: 基本仕様 §7.5:443 (「一意に解決できない場合はバンドルを生成せず、候補のいずれも選択しない…曖昧な場合は MISMATCH」) and :447; the counter-reading \"these are bundle-time labels, not the check value\" is killed by 詳細設計 §8.1:1056 (「記録する`impl_consistency`は解決失敗の種別で分ける」) and the §11.1:1339 check-item table (「曖昧な解決（E-SCAN-011）はMISMATCH」). CheckValue has both Mismatch and Missing (vtest-model/src/lib.rs:623-632), so neither is unrepresentable. (d) Code at HEAD e126bde matches every citation: vtest-scan/src/lib.rs:72-78 `.find()`; :1319-1334 E-SCAN-004 on count!=1; vtest-cli/src/lib.rs:1423-1433 `targets.first()` erroring only on None; :938-1007 run_audit_bundle attaches scan diagnostics but returns ok/exit 0; vtest-verify/src/lib.rs:649→1355-1441 derives impl_consistency solely from audit records. (a)/(b) Reachability and absence of an earlier gate proven empirically below.",
   "repro": "Independently re-reproduced in a clean context. Built `cargo build -p vtest-cli` (up to date at HEAD); ran only inside `mktemp -d` projects seeded from tests/fixtures/calc/m1/base; repo left untouched (git status empty).\n\nB) SRC-ID collision (known/also_known both `@vtest.src-id SRC-DUAL`, test target SRC-DUAL):\n1. `vtest scan --format json` → exit 1, diagnostics [E-SCAN-011, E-SCAN-011, E-SCAN-004]; both Source Targets retained; test retained.\n2. `vtest verify --items impl_consistency --test TEST-M1-CLEAN` → ok:false, impl_consistency = NOT_CHECKED, basis \"audit missing\". Never MISMATCH/MISSING.\n3. `vtest audit bundle --kind impl-consistency --test TEST-M1-CLEAN` → ok:true, exit 0; bundle target silently = `rust-cargo::src/lib.rs::also_known`.\n4. PASS submit → accepted:true, exit 0.\n5. `vtest verify --items impl_consistency` → ok:true, exit 0, impl_consistency = PASS. Unscoped `vtest verify` also shows impl_consistency PASS and test_traceability PASS.\n\nA) cfg-gated duplicate locator: same outcome; bundle picked the `cfg(feature=\"left\")` body.",
   "contamination_check": "Not tainted. The contamination overwrote the repo-root `.verify/config.yaml` only; that file is now the SpecTracer config and `git status --porcelain` is empty, so `tests/fixtures/calc/m1/base` (the dossier's repro seed) and all of `crates/` are unmodified. The dossier's repro ran in scratchpad copies of that fixture, never in the repo root, so the contaminated root config was never on any invocation's path. Decisively, I re-ran both flavors from scratch in fresh `mktemp -d` projects in the current restored context and obtained byte-identical outcomes (NOT_CHECKED with no audit; auto-selected `also_known`; PASS/exit 0 after a PASS submit). The original repro context is therefore moot.",
   "notes": "No chain link is factually wrong. Points 1-8 all survive; the dossier already self-discloses that the source candidate's \"produces no bundle\" wording is wrong (a bundle IS produced) — an understatement, not an overturn. Residual spec tension: 詳細設計 §8.1 routes E-SCAN-004 to MISSING while §11.1/基本仕様 route the ambiguous case to MISMATCH, and the scanner emits E-SCAN-004 for both 0 and >1 candidates. This does not create UNCLEAR: the observed values (NOT_CHECKED, PASS) lie outside the union of every candidate expected value, and §7.5:447 explicitly forbids conflating 監査未実施 with 不一致. Cluster attribution HOLDS: the cluster's root_cause text (no arity discipline in `find_target_source`; diagnostics reach no check item) is exactly the mechanism verified line-by-line here; the cluster has two members, so NOT_APPLICABLE does not apply."
  },
  {
   "vo_id": "VO-ADAPTER-05",
   "verdict": "UPHELD",
   "cluster_attribution": "HOLDS",
   "refutation_summary": "All four attacks failed. (a) Reachability: my own repro drove register→scan→verify through public APIs with zero edits to the three crates; scan materialized 1 synthetic Test + 1 Source Target. (b) Earlier gate: the identical record with only the hash swapped returns PASS, so the line-1815 comparison is the sole discriminator. Structurally stronger: hash_execution_state_subject binds schema_id (vtest-model/src/lib.rs:908-913) while re-derivation hardwires \"rust-cargo-execution-state-v1\" (vtest-adapter-rust/src/runner.rs:491,502-521) plus the coverage rule runner.kind != \"cargo-llvm-cov\" (vtest-verify/src/lib.rs:1814-1815) — PASS is unreachable for any honest non-Rust record; and if an earlier gate rejected instead, the outcome is still non-PASS, so the claim breaks either way. (c) Expected: 詳細設計 v0.1:1390 item 4 requires 同じschema reconstruction verbatim; W7 \"STALE after hash change\" presupposes fresh = non-STALE. (d) Code unchanged since the dossier commit (c493432..HEAD touches no crates).",
   "repro": "Independent, fully outside the repo. Temp workspace <HOME>\\AppData\\Local\\Temp\\claude\\reverify-nBejx0\\repro, a new crate with path deps on the repo's UNMODIFIED committed crates (vtest-model/-adapter-api/-scan/-store/-verify); CARGO_TARGET_DIR in temp; repo tree clean (git status --porcelain empty). It implements a synthetic SourceDiscoveryAdapter (source/cases.synth, construct \"scenario adding two values\", metadata/tests.txt, locators scenario[...]/component[add]), inits a fresh temp project, calls scan_project_with_discovery, writes Evidence YAML in vtest-exec's writer shape, then verify_project scope [\"evidence_validity\"].\nOutput: scan tests=1 sources=1 adapter=synthetic; diagnostics [\"E-SCAN-003\"] (unregistered VO, incidental).\nA (honest synthetic subject sha256:8d9c0f…b96e) → evidence_validity = Stale.\nB (same record, rust-derived sha256:d563d9…b03e) → Pass.\nMatches the dossier's A=STALE / B=PASS pattern exactly.",
   "contamination_check": "Not tainted, affirmatively. The contamination was the repo root's .verify/config.yaml and Cargo.toml; the dossier's repro ran against a throwaway vtest-init project plus copied crate sources and `git show HEAD:Cargo.toml`, so the clobbered root config never entered its verify root. I re-established both legs independently on the now-restored clean tree (working tree clean; crate sources byte-identical to the dossier's commit) with a freshly created temp project of my own, and obtained the same STALE/PASS split. Hash values differ from the dossier's only because my synthetic draft's inputs differ — the discriminating behavior is identical. TAINTED's condition (\"could not independently re-establish\") is therefore defeated.",
   "notes": "Cluster split: the cluster's closing half (rust_cargo_execution_state_hash re-derivation + Rust coverage rule) is exactly what produces this member's defect; the opening half (record.adapter absent → Stale, compat channel never read) belongs to VO-ADAPTER-10 / VO-EXEC-12, not to this member. So the stated root cause does produce this defect.\nMinor slip, not chain-breaking: the dossier's condition says the committed fixture's metadata is metadata/tests.txt; tests/fixtures/adapters/synthetic/manifest.json actually says metadata/tests.json (tests.txt is the in-crate vtest-scan test fixture). Illustrative parenthetical only.\nOut of scope but observed: case B passed with revision.commit \"deadbeef\", i.e. evidence_record_validity only checks commit non-null, never compares it to current HEAD as 詳細設計:1390 item 3 requires — a separate defect.\nCorroborating leak confirmed: vtest-scan/src/lib.rs:415 hands every injected adapter rust_cargo_discovery_projection."
  },
  {
   "vo_id": "VO-AGG-03",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachable: read_record_ids (crates/vtest-store/src/lib.rs:564) accepts any *.yaml stem, so any unreadable file declaring kind: static enters `malformed` (vtest-verify/src/lib.rs:706-722). (b) No earlier gate: verify completes, diagnostics is empty, exit 1. (c) The one defensible counter-reading — \"UNKNOWN is item-level 判定不能, not a coexisting child value, so §4.3 doesn't govern\" — dies on 詳細設計 §3.6 L422, which prescribes exclusion of malformed records and STALE/NOT_CHECKED when no valid record remains, never item UNKNOWN; a sweep of every UNKNOWN/判定不能 occurrence in docs/ authorizes UNKNOWN only for analysis limits, never unreadable records. The code comment at L825-827 is rationale, not normative. (d) Current code matches verbatim: L828-836 filter, L842 `if project_value != CheckValue::Fail { project_value = CheckValue::Unknown }`, combine_values L1854-1876 with Unknown=2 as minimum. No test guards the override path.",
   "repro": "Independent, fresh: `cargo build -p vtest-cli` at HEAD f5053d6 (up-to-date, so binary provably matches read code); binary copied to `mktemp -d` = /tmp/tmp.MULOtH5ZHX (outside repo); all commands run there. Repo untouched (git status clean; only target/ written). Project: vtest init, demo crate with TEST-DEMO-1 (@vtest.target src/lib.rs::add), vo add VO-DEMO-1.\nCASE C control: static_audit=NOT_CHECKED, basis [\"Test TEST-DEMO-1: NotChecked\"]. Adding .verify/audits/A-BROKEN-1.yaml (id/kind: static/this_is:) → UNKNOWN, both grounds in basis, exit 1.\nCASE B: after `audit static` + appending a second assert, control = STALE [\"Test TEST-DEMO-1: Stale; 1 stale record(s) ignored\"]; with A-BROKEN-1 → UNKNOWN, basis lists both. Text report shows `static_audit UNKNOWN` and `Result: UNKNOWN` where §4.3 mandates STALE. A/B toggle of one file is the only variable.",
   "contamination_check": "Not tainted, on two independent grounds. (1) Mechanism: find_project_root (crates/vtest-store/src/lib.rs:534-546) walks cwd's ancestors and stops at the first directory containing .verify. Both the original repro and mine ran under %TEMP%, which is not under <REPO>, so the repo-root .verify/config.yaml — the contaminated file — was unreachable; the scratch project's own init-created config was used. The contamination was data, not code, so it could not alter the binary either. (2) Moot regardless: I re-established CASE B and CASE C from scratch in a new mktemp -d with a HEAD-matched build, reproducing the dossier's exact values and basis strings. The dossier's one real evidentiary weakness — it reused a prebuilt binary \"as-is (no rebuild)\" — is now closed.",
   "notes": "Singleton rationale in dogfood-rootcause-clusters.json (\"evaluate_static_audit's unassociated-malformed override at crates/vtest-verify/src/lib.rs:828-854 assigns Unknown directly, bypassing combine_values\") is accurate line-for-line against current code; NOT_APPLICABLE per singleton convention. Secondary observations, not part of the upheld chain: (i) the per-Test malformed branch at lib.rs:789-790 (Unknown when malformed_for_test is non-empty) is frozen by a test at lib.rs:2414-2416, so it is an intentional divergence from 詳細設計 §3.6 L422 rather than an omission — the verdict rests solely on the unassociated override, which no test guards; (ii) the tree renders the project-level item value on the per-Test row, so the wrong representative propagates to every row, reinforcing chain point 8. Scope caveat: repro used --items static_audit; full-scope runs fold the same item value, so the conclusion is unaffected."
  },
  {
   "vo_id": "VO-EXEC-07",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachability: scan admits two targets under @vtest.kind integration-normal (guarded by TEST-SCAN-015, crates/vtest-scan/src/lib.rs:2036-2085); I re-ran scan -> tests:1, no diagnostics, no E-SCAN-004 for the cfg-excluded fn (discovery.rs has no cfg handling, so ghost resolves statically). (b) No earlier/later gate: single call site runner.rs:702; vtest-exec/src/lib.rs:184 stores verbatim; records.rs:1188 reads the scalar verbatim; verify lib.rs:1667-1672 passes through; pure_static_fold (records.rs:980-989, FAIL-first) is Audit-only (used only in vtest-audit/src/lib.rs:264); target entries are read in verify only for the DA-002 PASS rescue (lib.rs:988-991). (c) Spec re-read verbatim: 基本仕様 §7.9:499 and 詳細設計 §10.2:1304-1307 both order FAIL before UNKNOWN; §10.2:1310 makes the persisted Evidence aggregate the spec's fold. VO claim text (.verify/vo/VO-EXEC-07.yaml:13) matches point 1 verbatim. (d) Code still inverted at runner.rs:225-238, contradicting its own doc comment (runner.rs:196-199).",
   "repro": "Fully re-reproduced outside the repo. mktemp -d -> /tmp/tmp.TtgT0YOprJ/vo7chk (cargo lib vo7chk); src/lib.rs = pub fn never_called + #[cfg(feature=\"absent\")] pub fn ghost; tests/multi.rs = one #[test] with @vtest.id TEST-VO7-MULTI, two @vtest.target lines, @vtest.kind integration-normal. Binary: freshly built <REPO>/target/debug/vtest.exe (cargo build -p vtest-cli, exit 0); every command used --project <temp>. init produced a fresh config v2 with run.coverage: llvm-cov. scan -> OK {\"files\":2,\"sources\":3,\"tests\":1}. git init + commit (with .verify/ ignored, else evidence_validity is STALE — an omission in the dossier's repro text). run --all -> OK, diagnostics [], target_execution {result: UNKNOWN, targets:[never_called FAIL/0, ghost UNKNOWN/null]}. verify -> target_execution UNKNOWN, evidence_validity PASS, test node NOT_CHECKED, Result: MISSING. Identical to the dossier.",
   "contamination_check": "Not contaminated. The repo-root .verify/config.yaml can only affect runs whose project root is the repo; the dossier's repro and mine both ran with --project pointed at a fresh temp project whose config was written by that project's own init (verified: version 2, adapters[rust-cargo].run.coverage: llvm-cov, full verify scope — not a calc-m1-base fixture). The defect is also config-independent: the inverted fold at runner.rs:225-238 is unconditional on the non-fast coverage path, with no config-derived branch. My re-run used a binary rebuilt from current HEAD and reproduced every value the dossier reports, so the evidence is re-established independently of the contaminated context.",
   "notes": "Singleton, so cluster_attribution is NOT_APPLICABLE; the singleton rationale in dogfood-rootcause-clusters.json notes (\"the adapter's per-target fold ... tests UNKNOWN before FAIL, inverting the fixed priority\") is exactly what I verified, and the note's decision to keep it separate from target-execution-entry-set-never-read is correct — the persisted Evidence aggregate is wrong independently of any verify-side rewrite. Non-material inaccuracies: the fold is at runner.rs:225-238 (dossier says 224-237); 基本仕様 §4.3's representative priority is at :219 (dossier says :220); the repro text omits that .verify/ must be git-ignored for evidence_validity PASS. One scope caveat: OK/NG is NG under both the actual and the spec-mandated value, so the defect changes the reported value and the representative severity, not the binary verdict — consistent with the dossier's own negation (b)."
  },
  {
   "vo_id": "VO-SEMAUDIT-05",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (a) Reachable with the shipped CLI alone: two `audit submit` calls write independent records (crates/vtest-cli/src/lib.rs:1134-1160, new_record_id, no dedup/supersede). (b) No earlier gate: submit only checks bundle/kind/subjects/verdict/reasons; at verify both records were valid (no \"(stale)\" in basis). (c) Spec reading holds: §8.5 L1187 verbatim, §3.6 L385-386 restates \"latest valid record\" and names only FAIL/PASS mixing as fail-closed, §11.1 L1338 binds semantic_audit to §8.5, so \"合成 = severity max\" is unavailable. The strongest counter — L963's \"never promote UNKNOWN to PASS\" — misses: under §8.5 the superseded UNKNOWN never becomes an item value. (d) Code matches every citation: lib.rs:648, 1355-1442, fold at 1414-1425, combine_values 1854-1876, compare_audit_recency 1115 called only at 936/962 (static path); TEST-VERIFY-008/015 exist at lib.rs:2435/2626.",
   "repro": "Independent, stronger than the dossier's (no timestamp hand-edit, plus an A/B control). Temp dir /tmp/tmp.uqa6CNraOh (outside repo); repo read-only, only `cargo build -p vtest-cli` (already up to date); `git status` clean after.\nSteps: git ls-files copy of tests/fixtures/calc/m1/base -> $T/proj; git init+commit (44d3edb); `test list` -> TEST-M1-CLEAN; `audit bundle --kind test-semantic --test TEST-M1-CLEAN --include-failed` then `audit submit` UNKNOWN (01M02W05JN..., audited_at 2026-08-15T14:07:49Z); repeat with PASS (01M02W0EEY..., 14:07:58Z). Identical subjects (test/target/vo hashes only).\nObserved: `report --items semantic_audit` -> semantic_audit UNKNOWN, result UNKNOWN, basis lists both ids without \"(stale)\".\nControl: move the older UNKNOWN record out -> semantic_audit PASS, result PASS. So the non-latest record dominates; spec demands PASS.",
   "contamination_check": "Not tainted. The contamination was the repo-root .verify/config.yaml overwritten with the calc-m1-base fixture config. The dossier's repro ran `--project <temp copy of that same fixture>`, so it could not inherit repo-root state, and the defect is in verify's record fold, not config. Decisively, I re-established the result independently: current HEAD (e126bde), freshly checked binary, a clean mktemp dir, no hand-edited audited_at, plus a control run. The observation is timestamp- and config-independent (evaluate_test_audit never reads audited_at). TAINTED is foreclosed.",
   "notes": "Singleton, so no cluster root cause to attribute; the singleton rationale in dogfood-rootcause-clusters.json is nonetheless factually accurate (combine_values severity-max fold, audited_at never consulted, compare_audit_recency existing and used only on the static path at lib.rs:936/962). The dossier's VO claim text matches dogfood-ontology-topdown-v2.json verbatim. Secondary scope claims (impl_consistency via lib.rs:649, vo-coverage fold near lib.rs:1330) share the same fold and were not separately reproduced; they are not load-bearing for the verdict."
  },
  {
   "vo_id": "VO-EXEC-06",
   "verdict": "UPHELD",
   "cluster_attribution": "NOT_APPLICABLE",
   "refutation_summary": "All four attacks failed. (d) Code at HEAD says exactly what the dossier claims: crates/vtest-verify/src/lib.rs:1630-1636 `_ => CheckValue::Stale` in evaluate_runtime; :1663-1665 `if evidence_record_validity(..) != Pass { Stale }` in evaluate_target_execution; the shared ladder (:1764-1819) correctly returns Mismatch/Unknown, and evaluate_test_execution/_evidence_validity propagate it. (c) Spec is unambiguous and triply stated: 基本仕様:489, 詳細設計 §11.2 (\"`evidence_validity`と同じMISMATCH/STALE/UNKNOWNを保持する\"), 別紙C:136. (b) No earlier gate: read_evidence (crates/vtest-store/src/records.rs:1051+) validates neither adapter id nor `complete`. (a) Path is reachable and dispatched (:650-653); I hit it with an UNTAMPERED Evidence record. Binary rebuilt from HEAD (cargo build -p vtest-cli, no-op).",
   "repro": "Isolated: mktemp -d (/tmp/tmp.jVaXTybfn2), copied tests/fixtures/calc/m1/base (target/ excluded) + target/debug/vtest.exe; git init/commit; repo read-only throughout; temp dir deleted after.\n1. run --all --fast -> Evidence for TEST-M1-CLEAN, adapter rust-cargo, complete true, checked false.\n2. verify -> PASS / PASS / PASS / NOT_CHECKED.\n3. complete: true->false -> evidence_validity=UNKNOWN, test_execution=UNKNOWN, runtime_result=STALE, target_execution=STALE.\n4. adapter rust-cargo->python-pytest -> MISMATCH, MISMATCH, STALE, STALE.\n5. Extra (no record edit): restored record, made src/notes.txt unreadable via icacls deny so the current snapshot cannot be rebuilt -> UNKNOWN, UNKNOWN, STALE, STALE. Per-Test basis strings show the same split.",
   "contamination_check": "Not tainted. (a) My repro is structurally independent of the incident: fresh temp copy of the fixture, binary rebuilt from HEAD, `--project` pinned to the temp dir, and repo `.verify/config.yaml` verified to be the SpecTracer config and git-clean. (b) The contamination was the repo root's config being replaced by the calc-m1-base fixture config; the dossier's repro ran against its own temp copy of that same fixture, so root state could not have produced its numbers. (c) The decisive signal is a within-run divergence (evidence_validity=UNKNOWN while runtime_result=STALE for one record), which the code predicts deterministically and no config can manufacture. Step 5 reproduces it with an untouched Evidence file.",
   "notes": "Singleton, so cluster_attribution is NOT_APPLICABLE; its stated rationale in dogfood-rootcause-clusters.json notes (\"evaluate_runtime `_ => Stale` and evaluate_target_execution `!= Pass -> Stale` collapse the one freshness verdict, unlike evaluate_test_execution\") is verified accurate and is exactly the mechanism I observed. One nuance in chain point 8(2): \"adapter retargeted by ordinary means\" is generous — test.execution.adapter is always the discovery adapter today and vtest-scan/src/lib.rs:230 diagnoses managed-vs-discovery divergence, so MISMATCH currently arises via record edit or an adapter migration. Point 4 is disjunctive and the UNKNOWN leg is fully established without tampering, so the verdict is unaffected. Confirmed no test in crates/ asserts runtime_result/target_execution for invalid Evidence (TEST-CLI-034 asserts evidence_validity only)."
  }
 ]
}