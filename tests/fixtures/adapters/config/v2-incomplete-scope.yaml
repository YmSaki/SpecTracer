version: 2
project:
  name: adapter-acceptance-invalid
adapters:
  - id: rust-cargo
    roots: ["."]
    scan:
      include: [src, tests]
      assertion_macros: []
    run:
      coverage: off
verify:
  full_scope: [spec_coverage, vo_decomposition, vo_coverage, test_existence, static_audit, semantic_audit, impl_consistency, test_execution, runtime_result, target_execution, evidence_validity]
