version: 1
project:
  name: SpecTracer
scan:
  include:
    - crates
  assertion_macros: []
verify:
  full_scope:
    - spec_coverage
    - vo_decomposition
    - vo_coverage
    - test_existence
    - static_audit
    - semantic_audit
    - impl_consistency
    - test_execution
    - runtime_result
    - target_execution
    - evidence_validity
run:
  coverage: llvm-cov
